package main

// "golang.org/x/tour/wc"

func main() {
	// goroutines.go
	mainRoutine()
	// methods.go
/*
	vPtr := VertexFloat{3, 4}
	fmt.Printf("Before scaling: %+v, Abs: %v\n", vPtr, vPtr.Abs())
	vPtr.Scale(10)
	fmt.Printf("After scaling: %+v, Abs: %v\n", vPtr, vPtr.Abs())

	getRectAreaPerimemter()
	v := VertexFloat{3.5, 4.5}
	fmt.Println(v.Abs())
*/
	/*
	// maps.go
	printFibonnaci(10)
	closureExample()
	funcAsParam()
	wc.Test(WordCount)
	fmt.Println("")
	useMaps()
*/
	// slice in array.go
/*
	rangeIV()
	rangeSlice()
	ticTacToe()
	makingSlice()
	fmt.Println(justSlice)
	fmt.Println(justSliceStart)
	fmt.Println(justSliceEnd)

	justSlice = justSlice[:0]
	printSlice(justSlice)
	justSlice = justSlice[:4]
	printSlice(justSlice)
	justSlice = justSlice[2:]
	// the capacity automatically reduces due to reduction in length
	printSlice(justSlice)

	// array.go
	fmt.Println(arr)
	fmt.Println("BEFORE: ",primes)
	fmt.Println(slicePrime1)
	fmt.Println(slicePrime2)
	slicePrime1[4] = 31
	fmt.Println("AFTER: ",primes)
	fmt.Println(slicePrime1)
	fmt.Println(slicePrime2)

	// structs.go
	dog := struct {
		name   string
		isGood bool
	}{
		"Rex",
		true,
	}
	fmt.Println(dog)

	// The special prefix & returns a pointer to the struct value.
	fmt.Println(&Person{name: "John", age: 40})
	fmt.Println(Person{name: "Alice", age: 30})
	fmt.Println(Person{name: "Fred"})

	n1 := newPerson("Rohit", 37, Vertex{55, 15})
	n1.ChangeAge(29)
	fmt.Println(n1)
	n2 := NNewPerson("George")
	fmt.Println(n2)

	fmt.Println(n2.Vertex.X)
	fmt.Println(n2.Y)
	// structs fields
	v := Vertex{1, 2}
	fmt.Println("X before:", v.X)
	v.X = 4
	fmt.Println("X after:", v.X)

	// need atleast one float value on RHS so that output is 2.5, otherwise 5/2 = 2
	var y float64 = 5 / 2.00
	fmt.Println(y)
	*/
}
